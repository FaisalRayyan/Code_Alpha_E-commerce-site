document.addEventListener("DOMContentLoaded",()=>{const loader=document.querySelector(".brand-loader");const product=document.querySelector(".motion-hero__product");const items=document.querySelectorAll("[data-reveal]");const reduced=matchMedia("(prefers-reduced-motion: reduce)").matches;addEventListener("load",()=>setTimeout(()=>loader?.classList.add("is-hidden"),500));const observer=new IntersectionObserver(entries=>entries.forEach(entry=>{if(entry.isIntersecting){entry.target.classList.add("is-visible");observer.unobserve(entry.target)}}),{threshold:.18});items.forEach(item=>observer.observe(item));if(!reduced&&product)addEventListener("scroll",()=>{const p=Math.min(scrollY/innerHeight,1.5);product.style.transform=`translate3d(0,${p*72}px,0) rotate(${-4+p*7}deg) scale(${1-Math.min(p*.05,.06)})`},{passive:true})});
